[project]
name = "dsa-toolkit"
version = "0.1.0"
description = "Implementations of fundamental data structures and algorithms"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
    "pytest",
    "matplotlib",
]

[tool.pytest.ini_options]
testpaths = ["tests"]